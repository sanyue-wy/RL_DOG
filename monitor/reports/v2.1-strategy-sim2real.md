# v2.1 策略选择与Sim2Real功能技术实现报告

> 版本：v2.1 | 日期：2026-07-31 | 状态：📋 待审阅

---

## 一、版本概述

### 1.1 版本目标

在现有训练监控系统基础上，新增以下功能：

1. **策略选择功能** - 支持在UI界面选择不同的训练任务/策略
2. **Sim2Real按钮** - 一键启动Sim-to-Sim验证流程
3. **一键训练优化** - 简化训练启动流程，提升用户体验

### 1.2 改版范围

| 维度 | 范围 |
|------|------|
| 后端 | 新增策略配置API、Sim2Real进程管理 |
| 前端 | TrainingControl组件重构、新增策略选择和Sim2Real按钮 |
| 数据流 | 新增策略配置传递、Sim2Real状态监控 |

---

## 二、技术架构设计

### 2.1 整体架构

```
┌─────────────────────────────────────────────────────────────────┐
│  前端 (React)                                                    │
│  ┌─────────────────────────────────────────────────────────────┐ │
│  │  TrainingControl (重构)                                      │ │
│  │  ├── 策略选择下拉菜单                                         │ │
│  │  ├── 一键训练按钮 (整合渲染模式)                               │ │
│  │  ├── Sim2Real按钮                                           │ │
│  │  └── 原有控制按钮 (暂停/停止)                                  │ │
│  └─────────────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────────┘
                           │
                    WebSocket (Socket.IO)
                           │
┌─────────────────────────────────────────────────────────────────┐
│  后端 (FastAPI)                                                  │
│  ┌─────────────────────────────────────────────────────────────┐ │
│  │  TrainManager (扩展)                                         │ │
│  │  ├── 策略配置管理                                             │ │
│  │  ├── 多任务训练支持                                           │ │
│  │  └── Sim2Real进程管理                                        │ │
│  └─────────────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────────┘
                           │
┌─────────────────────────────────────────────────────────────────┐
│  数据层                                                          │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────────────┐      │
│  │ 策略配置    │  │ 训练进程    │  │  Sim2Real进程       │      │
│  │ (JSON)      │  │ (subprocess)│  │  (subprocess)       │      │
│  └─────────────┘  └─────────────┘  └─────────────────────┘      │
└─────────────────────────────────────────────────────────────────┘
```

### 2.2 数据流设计

```
用户操作                    前端                    后端                    执行层
   │                        │                      │                      │
   │ 选择策略               │                      │                      │
   │──────────────────────>│  emit('select_strategy', {task: 'rc'})       │
   │                        │─────────────────────>│  更新策略配置         │
   │                        │                      │─────────────────────>│
   │                        │                      │                      │
   │ 点击一键训练            │                      │                      │
   │──────────────────────>│  emit('start_training', {task, render})      │
   │                        │─────────────────────>│  start(task, render) │
   │                        │                      │─────────────────────>│ subprocess
   │                        │                      │                      │
   │ 点击Sim2Real           │                      │                      │
   │──────────────────────>│  emit('start_sim2real', {policy_path})       │
   │                        │─────────────────────>│  start_sim2real()    │
   │                        │                      │─────────────────────>│ subprocess
```

---

## 三、后端实现方案

### 3.1 TrainManager 扩展

#### 3.1.1 新增属性

```python
class TrainManager:
    def __init__(self, socketio=None):
        # ... 现有属性 ...
        
        # 新增：策略配置
        self.available_tasks = {
            'rc': {
                'name': 'RC Blind Plane',
                'description': '12-DOF 四足机器人平地训练',
                'config': 'RCBlindPlaneCfg',
                'ppo_config': 'RCBlindPlaneCfgPPO',
                'env_class': 'RCBlindPlane'
            },
            'rc_terrain': {
                'name': 'RC Blind Terrain',
                'description': '12-DOF 四足机器人地形训练',
                'config': 'RCBlindTerrainCfg',
                'ppo_config': 'RCBlindTerrainCfgPPO',
                'env_class': 'RCBlindTerrain'
            },
            'a1': {
                'name': 'Unitree A1',
                'description': 'A1 四足机器人训练',
                'config': 'A1RoughCfg',
                'ppo_config': 'A1RoughCfgPPO',
                'env_class': 'LeggedRobot'
            },
            'go1': {
                'name': 'Unitree Go1',
                'description': 'Go1 四足机器人训练',
                'config': 'Go1RoughCfg',
                'ppo_config': 'Go1RoughCfgPPO',
                'env_class': 'LeggedRobot'
            }
        }
        self.current_task = 'rc'  # 默认任务
        
        # 新增：Sim2Real 进程
        self.sim2real_process = None
        self.sim2real_running = False
```

#### 3.1.2 新增方法

```python
def get_available_tasks(self):
    """获取可用任务列表"""
    return self.available_tasks

def set_current_task(self, task_name):
    """设置当前任务"""
    if task_name in self.available_tasks:
        self.current_task = task_name
        return True, f"已切换到任务: {self.available_tasks[task_name]['name']}"
    return False, f"未知任务: {task_name}"

def start(self, render=False, task=None):
    """启动训练（扩展版）"""
    if self.running:
        return False, "训练已在运行中"
    
    # 使用指定任务或当前任务
    target_task = task or self.current_task
    
    if target_task not in self.available_tasks:
        return False, f"未知任务: {target_task}"
    
    # 获取事件循环
    try:
        self.loop = asyncio.get_event_loop()
    except RuntimeError:
        self.loop = asyncio.new_event_loop()
        asyncio.set_event_loop(self.loop)
    
    cmd = [
        sys.executable,
        "-u",  # 无缓冲模式
        str(self.project_root / "legged_gym" / "legged_gym" / "scripts" / "train.py"),
        "--task", target_task
    ]
    
    if not render:
        cmd.append("--headless")
    
    try:
        self.process = subprocess.Popen(
            cmd,
            stdout=subprocess.PIPE,
            stderr=subprocess.STDOUT,
            universal_newlines=True,
            bufsize=0,  # 无缓冲
            cwd=str(self.project_root),
            env={**os.environ, 'PYTHONUNBUFFERED': '1'}
        )
        self.running = True
        self.render_mode = render
        self.paused = False
        self.current_task = target_task
        self.start_time = datetime.now()
        
        # 启动输出读取线程
        thread = threading.Thread(target=self._read_output, daemon=True)
        thread.start()
        
        task_name = self.available_tasks[target_task]['name']
        print(f"[TrainManager] 训练已启动 (task={target_task}, render={render})")
        return True, f"训练已启动 - {task_name} (渲染: {'开启' if render else '关闭'})"
    except Exception as e:
        print(f"[TrainManager] 启动失败: {e}")
        return False, f"启动失败: {str(e)}"

def start_sim2real(self, policy_path=None):
    """启动 Sim2Real 验证"""
    if self.sim2real_running:
        return False, "Sim2Real 验证已在运行中"
    
    # 默认策略路径
    if policy_path is None:
        policy_path = str(self.project_root / "legged_gym" / "logs" / "blindplane" / "latest" / "model_1200.pt")
    
    cmd = [
        sys.executable,
        "-u",
        str(self.project_root / "legged_gym" / "sim2sim.py")
    ]
    
    try:
        self.sim2real_process = subprocess.Popen(
            cmd,
            stdout=subprocess.PIPE,
            stderr=subprocess.STDOUT,
            universal_newlines=True,
            bufsize=0,
            cwd=str(self.project_root / "legged_gym"),
            env={**os.environ, 'PYTHONUNBUFFERED': '1'}
        )
        self.sim2real_running = True
        
        # 启动输出读取线程
        thread = threading.Thread(target=self._read_sim2real_output, daemon=True)
        thread.start()
        
        print("[TrainManager] Sim2Real 验证已启动")
        return True, "Sim2Real 验证已启动"
    except Exception as e:
        print(f"[TrainManager] Sim2Real 启动失败: {e}")
        return False, f"Sim2Real 启动失败: {str(e)}"

def stop_sim2real(self):
    """停止 Sim2Real 验证"""
    if not self.sim2real_running:
        return False, "Sim2Real 验证未运行"
    
    try:
        if self.sim2real_process:
            self.sim2real_process.terminate()
            try:
                self.sim2real_process.wait(timeout=5)
            except subprocess.TimeoutExpired:
                self.sim2real_process.kill()
        self.sim2real_running = False
        return True, "Sim2Real 验证已停止"
    except Exception as e:
        return False, f"停止失败: {str(e)}"

def _read_sim2real_output(self):
    """读取 Sim2Real 输出"""
    try:
        print("[TrainManager] Sim2Real 输出读取线程已启动")
        for line in iter(self.sim2real_process.stdout.readline, ''):
            if line:
                line = line.strip()
                print(f"[Sim2Real] {line[:100]}")
                self._emit_sync('sim2real_output', {'data': line})
        
        self.sim2real_running = False
        self._emit_sync('sim2real_status', {'status': 'finished'})
        print("[TrainManager] Sim2Real 输出读取线程已结束")
    except Exception as e:
        print(f"[TrainManager] Sim2Real 输出读取错误: {e}")
        self._emit_sync('sim2real_output', {'data': f'Error: {str(e)}'})

def get_status(self):
    """获取训练状态（扩展版）"""
    elapsed = 0
    if self.start_time and self.running:
        elapsed = (datetime.now() - self.start_time).total_seconds()
    
    return {
        'running': self.running,
        'paused': self.paused,
        'render_mode': self.render_mode,
        'current_iteration': self.current_iteration,
        'total_iterations': self.total_iterations,
        'start_time': self.start_time.isoformat() if self.start_time else None,
        'elapsed_seconds': round(elapsed, 1),
        # 新增
        'current_task': self.current_task,
        'current_task_name': self.available_tasks.get(self.current_task, {}).get('name', 'Unknown'),
        'sim2real_running': self.sim2real_running
    }
```

### 3.2 app.py 扩展

#### 3.2.1 新增 Socket.IO 事件

```python
@sio.on('get_tasks')
async def handle_get_tasks(sid):
    """获取可用任务列表"""
    tasks = train_manager.get_available_tasks()
    await sio.emit('tasks_list', {'tasks': tasks}, to=sid)

@sio.on('select_task')
async def handle_select_task(sid, data):
    """选择训练任务"""
    task_name = data.get('task', 'rc')
    success, message = train_manager.set_current_task(task_name)
    await sio.emit('task_selected', {
        'success': success,
        'message': message,
        'current_task': train_manager.current_task
    })

@sio.on('start_sim2real')
async def handle_start_sim2real(sid, data):
    """启动 Sim2Real 验证"""
    policy_path = data.get('policy_path', None)
    success, message = train_manager.start_sim2real(policy_path)
    await sio.emit('sim2real_status', {
        'status': 'started' if success else 'error',
        'message': message,
        'sim2real_running': train_manager.sim2real_running
    })

@sio.on('stop_sim2real')
async def handle_stop_sim2real(sid):
    """停止 Sim2Real 验证"""
    success, message = train_manager.stop_sim2real()
    await sio.emit('sim2real_status', {
        'status': 'stopped' if success else 'error',
        'message': message,
        'sim2real_running': train_manager.sim2real_running
    })
```

---

## 四、前端实现方案

### 4.1 TrainingControl 组件重构

#### 4.1.1 新增状态和Props

```jsx
import { Play, Pause, Square, Eye, EyeOff, Gamepad2, Settings, Zap } from 'lucide-react'
import { useState, useEffect } from 'react'

export function TrainingControl({ 
  status, 
  onStart, 
  onStop, 
  onPause,
  // 新增 Props
  tasks,
  currentTask,
  onTaskChange,
  onStartSim2Real,
  onStopSim2Real,
  sim2realRunning
}) {
  const { running, paused, render_mode, current_iteration, total_iterations } = status
  const [selectedRender, setSelectedRender] = useState(false)
  
  return (
    <div className="bg-card rounded-lg border p-4">
      <h2 className="text-lg font-semibold mb-4 text-card-foreground">训练控制</h2>
      
      {/* 策略选择 */}
      <div className="mb-4">
        <label className="text-sm font-medium text-muted-foreground mb-2 block">
          选择策略
        </label>
        <select
          value={currentTask}
          onChange={(e) => onTaskChange(e.target.value)}
          disabled={running}
          className="w-full px-3 py-2 rounded-md border border-input bg-background text-sm disabled:opacity-50 disabled:cursor-not-allowed"
        >
          {Object.entries(tasks).map(([key, task]) => (
            <option key={key} value={key}>
              {task.name} - {task.description}
            </option>
          ))}
        </select>
      </div>
      
      {/* 一键训练按钮 */}
      <div className="mb-4">
        <button
          onClick={() => onStart(selectedRender)}
          disabled={running}
          className="w-full flex items-center justify-center gap-2 px-4 py-3 rounded-md bg-gradient-to-r from-green-600 to-emerald-600 text-white hover:from-green-700 hover:to-emerald-700 disabled:opacity-50 disabled:cursor-not-allowed transition-all font-medium"
        >
          <Zap className="w-5 h-5" />
          <span>一键训练</span>
        </button>
        
        {/* 渲染模式选择 */}
        <div className="flex items-center gap-2 mt-2">
          <input
            type="checkbox"
            id="render-mode"
            checked={selectedRender}
            onChange={(e) => setSelectedRender(e.target.checked)}
            disabled={running}
            className="rounded border-input"
          />
          <label htmlFor="render-mode" className="text-sm text-muted-foreground">
            开启渲染模式
          </label>
        </div>
      </div>
      
      {/* Sim2Real 按钮 */}
      <div className="mb-4">
        <button
          onClick={sim2realRunning ? onStopSim2Real : onStartSim2Real}
          disabled={running}
          className={`w-full flex items-center justify-center gap-2 px-4 py-2 rounded-md transition-colors ${
            sim2realRunning 
              ? 'bg-orange-600 text-white hover:bg-orange-700'
              : 'bg-purple-600 text-white hover:bg-purple-700'
          } disabled:opacity-50 disabled:cursor-not-allowed`}
        >
          <Gamepad2 className="w-4 h-4" />
          <span>{sim2realRunning ? '停止 Sim2Real' : '启动 Sim2Real'}</span>
        </button>
      </div>
      
      {/* 分隔线 */}
      <div className="border-t my-4" />
      
      {/* 原有控制按钮 */}
      <div className="grid grid-cols-2 gap-3 mb-4">
        <button
          onClick={onPause}
          disabled={!running}
          className="flex items-center justify-center gap-2 px-4 py-2 rounded-md border border-input bg-background hover:bg-accent hover:text-accent-foreground disabled:opacity-50 disabled:cursor-not-allowed transition-colors"
        >
          <Pause className="w-4 h-4" />
          <span>{paused ? '恢复' : '暂停'}</span>
        </button>
        
        <button
          onClick={onStop}
          disabled={!running}
          className="flex items-center justify-center gap-2 px-4 py-2 rounded-md bg-destructive text-destructive-foreground hover:bg-destructive/90 disabled:opacity-50 disabled:cursor-not-allowed transition-colors"
        >
          <Square className="w-4 h-4" />
          <span>停止</span>
        </button>
      </div>
      
      {/* 状态显示 */}
      <div className="space-y-2">
        <div className="flex items-center justify-between text-sm">
          <span className="text-muted-foreground">状态</span>
          <span className={`flex items-center gap-1.5 ${running ? 'text-green-600' : 'text-muted-foreground'}`}>
            <span className={`w-2 h-2 rounded-full ${running ? 'bg-green-500 animate-pulse' : 'bg-gray-400'}`} />
            {running ? (paused ? '已暂停' : '训练中') : '已停止'}
          </span>
        </div>
        
        <div className="flex items-center justify-between text-sm">
          <span className="text-muted-foreground">当前策略</span>
          <span className="font-medium text-foreground">
            {status.current_task_name || 'Unknown'}
          </span>
        </div>
        
        <div className="flex items-center justify-between text-sm">
          <span className="text-muted-foreground">渲染</span>
          <span className={render_mode ? 'text-green-600' : 'text-muted-foreground'}>
            {render_mode ? '开启' : '关闭'}
          </span>
        </div>
        
        <div className="flex items-center justify-between text-sm">
          <span className="text-muted-foreground">迭代</span>
          <span className="font-mono">
            {current_iteration.toLocaleString()} / {total_iterations.toLocaleString()}
          </span>
        </div>
        
        {/* 进度条 */}
        <div className="w-full bg-secondary rounded-full h-2 mt-2">
          <div
            className="bg-primary h-2 rounded-full transition-all duration-300"
            style={{ width: `${(current_iteration / total_iterations) * 100}%` }}
          />
        </div>
      </div>
    </div>
  )
}
```

### 4.2 App.jsx 扩展

#### 4.2.1 新增状态和事件处理

```jsx
function App() {
  // ... 现有状态 ...
  
  // 新增状态
  const [tasks, setTasks] = useState({})
  const [currentTask, setCurrentTask] = useState('rc')
  const [sim2realRunning, setSim2realRunning] = useState(false)
  
  // Socket.IO 事件监听
  useEffect(() => {
    // ... 现有事件 ...
    
    // 新增：任务列表
    socket.on('tasks_list', (data) => {
      setTasks(data.tasks)
    })
    
    // 新增：任务选择结果
    socket.on('task_selected', (data) => {
      if (data.success) {
        setCurrentTask(data.current_task)
      }
      addLog(data.message)
    })
    
    // 新增：Sim2Real 状态
    socket.on('sim2real_status', (data) => {
      setSim2realRunning(data.sim2real_running)
      addLog(data.message)
    })
    
    // 新增：Sim2Real 输出
    socket.on('sim2real_output', (data) => {
      addLog(`[Sim2Real] ${data.data}`)
    })
    
    // 获取任务列表
    socket.emit('get_tasks')
    
    return () => {
      socket.off('tasks_list')
      socket.off('task_selected')
      socket.off('sim2real_status')
      socket.off('sim2real_output')
    }
  }, [])
  
  // 事件处理函数
  const handleTaskChange = (taskName) => {
    socket.emit('select_task', { task: taskName })
  }
  
  const handleStartSim2Real = () => {
    socket.emit('start_sim2real', {})
  }
  
  const handleStopSim2Real = () => {
    socket.emit('stop_sim2real')
  }
  
  return (
    <div className="flex gap-4 p-4">
      {/* 左侧：固定宽度控制区 */}
      <div className="w-72 flex-shrink-0">
        <TrainingControl
          status={status}
          onStart={handleStart}
          onStop={handleStop}
          onPause={handlePause}
          // 新增 Props
          tasks={tasks}
          currentTask={currentTask}
          onTaskChange={handleTaskChange}
          onStartSim2Real={handleStartSim2Real}
          onStopSim2Real={handleStopSim2Real}
          sim2realRunning={sim2realRunning}
        />
      </div>
      
      {/* 右侧：可滚动数据区 */}
      <div className="flex-1 space-y-4 min-w-0">
        {/* ... 现有组件 ... */}
      </div>
    </div>
  )
}
```

---

## 五、UI界面设计

### 5.1 TrainingControl 布局

```
┌─────────────────────────────────────┐
│           训练控制                    │
├─────────────────────────────────────┤
│  选择策略                            │
│  ┌─────────────────────────────────┐│
│  │ RC Blind Plane - 平地训练    ▼ ││
│  └─────────────────────────────────┘│
│                                     │
│  ┌─────────────────────────────────┐│
│  │        ⚡ 一键训练              ││
│  └─────────────────────────────────┘│
│  ☐ 开启渲染模式                      │
│                                     │
│  ┌─────────────────────────────────┐│
│  │      🎮 启动 Sim2Real           ││
│  └─────────────────────────────────┘
│                                     │
│  ─────────────────────────────────  │
│                                     │
│  ┌──────────────┐ ┌──────────────┐ │
│  │    ⏸ 暂停    │ │    ⬜ 停止    │ │
│  └──────────────┘ └──────────────┘ │
│                                     │
│  状态:        ● 训练中              │
│  当前策略:    RC Blind Plane        │
│  渲染:        关闭                  │
│  迭代:        164 / 100,000        │
│  ████████░░░░░░░░░░░░ 0.16%       │
└─────────────────────────────────────┘
```

### 5.2 颜色方案

| 元素 | 颜色 | 说明 |
|------|------|------|
| 一键训练按钮 | 渐变绿色 (green-600 → emerald-600) | 主操作，突出显示 |
| Sim2Real按钮 | 紫色 (purple-600) | 次要操作 |
| 暂停按钮 | 边框样式 | 中性操作 |
| 停止按钮 | 红色 (destructive) | 危险操作 |
| 策略选择 | 默认输入框样式 | 表单元素 |

---

## 六、实现步骤

### 6.1 后端实现步骤

1. **修改 train_manager.py**
   - 新增 `available_tasks` 属性
   - 新增 `current_task` 属性
   - 新增 `sim2real_process` 和 `sim2real_running` 属性
   - 新增 `get_available_tasks()` 方法
   - 新增 `set_current_task()` 方法
   - 扩展 `start()` 方法支持 task 参数
   - 新增 `start_sim2real()` 方法
   - 新增 `stop_sim2real()` 方法
   - 新增 `_read_sim2real_output()` 方法
   - 扩展 `get_status()` 方法返回新字段

2. **修改 app.py**
   - 新增 `get_tasks` 事件处理
   - 新增 `select_task` 事件处理
   - 新增 `start_sim2real` 事件处理
   - 新增 `stop_sim2real` 事件处理

### 6.2 前端实现步骤

1. **修改 TrainingControl.jsx**
   - 新增策略选择下拉菜单
   - 新增一键训练按钮（带渐变效果）
   - 新增渲染模式复选框
   - 新增 Sim2Real 按钮
   - 重新布局控制区域
   - 新增状态显示字段

2. **修改 App.jsx**
   - 新增 `tasks`、`currentTask`、`sim2realRunning` 状态
   - 新增 Socket.IO 事件监听
   - 新增事件处理函数
   - 传递新 Props 到 TrainingControl

3. **重新构建前端**
   ```bash
   cd monitor/frontend
   npm run build
   cp dist/index.html ../static/index.html
   cp dist/assets/* ../static/assets/
   ```

---

## 七、技术细节

### 7.1 策略配置数据结构

```json
{
  "rc": {
    "name": "RC Blind Plane",
    "description": "12-DOF 四足机器人平地训练",
    "config": "RCBlindPlaneCfg",
    "ppo_config": "RCBlindPlaneCfgPPO",
    "env_class": "RCBlindPlane"
  },
  "rc_terrain": {
    "name": "RC Blind Terrain",
    "description": "12-DOF 四足机器人地形训练",
    "config": "RCBlindTerrainCfg",
    "ppo_config": "RCBlindTerrainCfgPPO",
    "env_class": "RCBlindTerrain"
  },
  "a1": {
    "name": "Unitree A1",
    "description": "A1 四足机器人训练",
    "config": "A1RoughCfg",
    "ppo_config": "A1RoughCfgPPO",
    "env_class": "LeggedRobot"
  },
  "go1": {
    "name": "Unitree Go1",
    "description": "Go1 四足机器人训练",
    "config": "Go1RoughCfg",
    "ppo_config": "Go1RoughCfgPPO",
    "env_class": "LeggedRobot"
  }
}
```

### 7.2 Socket.IO 事件清单

| 事件名 | 方向 | 数据 | 说明 |
|--------|------|------|------|
| `get_tasks` | 前端→后端 | {} | 请求任务列表 |
| `tasks_list` | 后端→前端 | {tasks: {...}} | 返回任务列表 |
| `select_task` | 前端→后端 | {task: string} | 选择任务 |
| `task_selected` | 后端→前端 | {success, message, current_task} | 任务选择结果 |
| `start_sim2real` | 前端→后端 | {policy_path?: string} | 启动Sim2Real |
| `stop_sim2real` | 前端→后端 | {} | 停止Sim2Real |
| `sim2real_status` | 后端→前端 | {status, message, sim2real_running} | Sim2Real状态 |
| `sim2real_output` | 后端→前端 | {data: string} | Sim2Real输出 |

### 7.3 训练启动命令

```bash
# 原有命令
python -u legged_gym/legged_gym/scripts/train.py --task rc --headless

# 扩展命令（支持多任务）
python -u legged_gym/legged_gym/scripts/train.py --task rc_terrain --headless
python -u legged_gym/legged_gym/scripts/train.py --task a1 --headless
python -u legged_gym/legged_gym/scripts/train.py --task go1 --headless
```

### 7.4 Sim2Real 启动命令

```bash
cd legged_gym
python -u sim2sim.py
```

---

## 八、测试计划

### 8.1 功能测试

| 测试项 | 测试步骤 | 预期结果 |
|--------|----------|----------|
| 策略选择 | 选择不同任务 | 下拉菜单显示正确，状态更新 |
| 一键训练 | 点击一键训练按钮 | 训练启动，状态显示正确 |
| Sim2Real | 点击Sim2Real按钮 | MuJoCo窗口打开，状态更新 |
| 暂停/恢复 | 训练中点击暂停/恢复 | 训练暂停/恢复，状态正确 |
| 停止 | 训练中点击停止 | 训练停止，状态重置 |

### 8.2 边界测试

| 测试项 | 测试步骤 | 预期结果 |
|--------|----------|----------|
| 重复启动 | 训练中再次启动 | 提示"训练已在运行中" |
| 未知任务 | 选择不存在的任务 | 提示"未知任务" |
| Sim2Real重复启动 | Sim2Real运行中再次启动 | 提示"已在运行中" |

---

## 九、风险评估

| 风险 | 可能性 | 影响 | 应对措施 |
|------|--------|------|----------|
| 任务配置错误 | 低 | 高 | 验证任务注册表 |
| Sim2Real进程管理 | 中 | 中 | 完善进程生命周期 |
| UI状态同步 | 低 | 中 | 完善事件处理 |
| 前端构建失败 | 低 | 低 | 保持依赖版本稳定 |

---

## 十、后续优化方向

| 方向 | 说明 |
|------|------|
| 策略配置持久化 | 保存用户选择的策略配置 |
| 训练历史对比 | 对比不同策略的训练结果 |
| Sim2Real监控 | 实时显示Sim2Real状态和指标 |
| 策略热切换 | 训练过程中切换策略（高级功能） |
| 批量训练 | 支持队列式批量训练 |

---

## 十一、附录

### 11.1 相关文件清单

| 文件 | 修改类型 | 说明 |
|------|----------|------|
| `monitor/backend/train_manager.py` | 修改 | 扩展训练管理器 |
| `monitor/backend/app.py` | 修改 | 新增Socket.IO事件 |
| `monitor/frontend/src/components/TrainingControl.jsx` | 修改 | 重构UI组件 |
| `monitor/frontend/src/App.jsx` | 修改 | 新增状态和事件 |
| `monitor/static/index.html` | 重新构建 | 前端构建产物 |
| `monitor/static/assets/*` | 重新构建 | 前端构建产物 |

### 11.2 依赖检查

```bash
# 后端依赖
cd monitor/backend
pip install -r requirements.txt

# 前端依赖
cd monitor/frontend
npm install
```

### 11.3 启动验证

```bash
# 启动后端
cd monitor/backend
python app.py

# 访问前端
# http://localhost:8000
```

---

**报告完成时间**: 2026-07-31  
**报告版本**: v2.1-draft  
**状态**: 待审阅
